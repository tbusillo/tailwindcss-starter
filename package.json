{
  "name": "tailwind-starter",
  "version": "1.0.0",
  "description": "An easy to use Tailwind CSS starter template",
  "scripts": {
    "watch": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tom Busillo",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.1.4"
  }
}
